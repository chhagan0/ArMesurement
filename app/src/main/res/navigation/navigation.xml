<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/arFragment">
    <fragment
        android:id="@+id/arFragment"
        android:name="com.example.armeasur.views.ArFragment"
        android:label="ArFragment" >
        <action
            android:id="@+id/action_arFragment_to_sceneViewFragment"
            app:destination="@id/sceneViewFragment" />
    </fragment>
    <fragment
        android:id="@+id/sceneViewFragment"
        android:name="com.cxzcodes.armeasure.views.SceneViewFragment"
        android:label="SceneViewFragment" />
    <fragment
        android:id="@+id/login"
        android:name="com.example.armeasur.Login"
        android:label="fragment_login"
        tools:layout="@layout/fragment_login" />
</navigation>
